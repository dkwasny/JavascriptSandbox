{
  "name": "javascript-sandbox",
  "version": "1.0.0",
  "description": "A place for me to mess with npm",
  "main": "index.js",
  "scripts": {
    "lint": "eslint src/index.js",
    "bundle": "rollup --format iife --output build/bundle.js src/index.js",
    "copy-html": "cp src/index.html build",
    "all": "npm run lint && npm run bundle && npm run copy-html"
  },
  "author": "dkwasny",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^4.4.1",
    "rollup": "^0.46.2"
  }
}
